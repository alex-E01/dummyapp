import { ViewproductPipe } from './viewproduct.pipe';

describe('ViewproductPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewproductPipe();
    expect(pipe).toBeTruthy();
  });
});
